server:
  port: 8888
  # Override: JWT_SECRET environment variable
  jwt_secret: "dev-only-secret-32chars-not-prod!!"

mysql:
  # Override: MYSQL_DSN environment variable
  dsn: "root:123@123qwe@tcp(127.0.0.1:3306)/e2eechat?charset=utf8mb4&parseTime=True&loc=Local"

redis:
  # Local development: Set to "memory" to use the memory version of Redis (miniredis), no installation required
  # For the production environment, fill in the actual address, such as "127.0.0.1:6379"
  addr: "memory"
  password: ""
  db: 0

turn:
  # Override: TURN_SECRET environment variable
  secret: "8642c022c77b01f463a84f842d074ccc500bb3d2b220249ee2cd02cd07a6af27"
  host: "m.yzs88.com"
  port: 3478

# CORS and WebSocket allowed sources (actual domain name must be filled in for production environment)
# Leave blank or fill in "*" to allow all sources (development use only)
jpush:
  app_key: "8ae2c1c5587c62b88ace4630"
  master_secret: "3c65dad5a20799c4b7787bf1"
  enabled: true

# Version information (the production environment is overwritten with the environment variables of docker-compose: APP_LATEST_VERSION, etc.)
version:
  latest: "1.0.0"
  min_supported: "1.0.0"
  url: "https://m.yzs88.com:8088"
  windows: "https://m.yzs88.com:8088/download/yunChat.exe"
  apk: "https://m.yzs88.com:8088/download/yunChat.apk"
  notes: ""

allowed_origins:
  - "https://m.yzs88.com:8088"
